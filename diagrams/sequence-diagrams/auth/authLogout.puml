@startuml Auth (LOGOUT)

title "Auth (LOGOUT)"
header Sequence Diagram — be-project-68-hause89
footer Page %page% of %lastpage%
autonumber

skinparam ParticipantPadding 20
skinparam BoxPadding 10

participant "Client" as client
participant "<<javaScript>>\n:server" as server
participant "<<router>>\n:auth" as routerAuth
participant "<<controllers>>\n:auth" as controllerAuth

client -> server ++ : GET /api/v1/auth/logout
server -> routerAuth ++ : app.use('/api/v1/auth', auth)
routerAuth -> controllerAuth ++ : logout()
controllerAuth -> controllerAuth : clear token cookie (token='none')
controllerAuth --> client -- : 200 { success, data: {} }

@enduml
